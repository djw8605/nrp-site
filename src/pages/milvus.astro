---
import Layout from '~/layouts/PageLayout.astro';
import MilvusPassword from '~/components/vue/MilvusPassword.vue';
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';

const metadata = {
  title: 'Milvus password',
};

---

<Layout metadata={metadata}>
  <WidgetWrapper containerClass="py-2 md:py-4 lg:py-6" title="Milvus password">
    <MilvusPassword client:only="vue" />
  </WidgetWrapper>
</Layout>