---
import WpItem from '~/components/blog/WpListItem.astro';
import type { Post } from '~/types';

export interface Props {
  posts: Array<Post>;
}

const { posts } = Astro.props;
---

<ul>
  {
    posts.map((post) => (
      <li class="mb-12 md:mb-20">
        <WpItem post={post} />
      </li>
    ))
  }
</ul>
